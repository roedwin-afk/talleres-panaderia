diff --git a/src/components/TarjetaTaller.astro b/src/components/TarjetaTaller.astro
index 945252f..fcc54fe 100644
--- a/src/components/TarjetaTaller.astro
+++ b/src/components/TarjetaTaller.astro
@@ -18,6 +18,7 @@ const {
     nivel,
     cuposDisponibles,
     precio,
+    precioPreventa,
     imagen,
     imagenAlt,
 } = taller.data;
@@ -32,10 +33,14 @@ const fechaFormateada = fecha.toLocaleDateString("es-SV", {
     month: "long",
 });
 
-const precioFormateado = new Intl.NumberFormat("en-US", {
+const formateadorUSD = new Intl.NumberFormat("en-US", {
     style: "currency",
     currency: "USD",
-}).format(precio);
+});
+const precioFormateado = formateadorUSD.format(precio);
+const precioPreventaFormateado = precioPreventa
+    ? formateadorUSD.format(precioPreventa)
+    : null;
 
 const etiquetaInstructor =
     instructorGenero === "f" ? "Instructora" : "Instructor";
@@ -117,7 +122,23 @@ const base = import.meta.env.BASE_URL;
             </li>
         </ul>
 
-        <div class="card-price">{precioFormateado}</div>
+        <div class="card-price-block">
+            {
+                precioPreventaFormateado ? (
+                    <>
+                        <span class="card-price-preventa">
+                            {precioPreventaFormateado}
+                            <span class="badge-preventa">Preventa</span>
+                        </span>
+                        <span class="card-price-regular">
+                            {precioFormateado} reservando con el 50%
+                        </span>
+                    </>
+                ) : (
+                    <span class="card-price-preventa">{precioFormateado}</span>
+                )
+            }
+        </div>
 
         <div class="card-actions">
             <a
@@ -213,12 +234,36 @@ const base = import.meta.env.BASE_URL;
         text-align: right;
     }
 
-    .card-price {
+    .card-price-block {
+        display: flex;
+        flex-direction: column;
+        align-items: flex-end;
+        margin-bottom: var(--space-sm);
+    }
+
+    .card-price-preventa {
+        display: inline-flex;
+        align-items: center;
+        gap: 0.4rem;
         font-size: 1.5rem;
         font-weight: 700;
         color: var(--brand-primary);
-        text-align: right;
-        margin-bottom: var(--space-sm);
+    }
+
+    .badge-preventa {
+        font-size: 0.65rem;
+        font-weight: 700;
+        text-transform: uppercase;
+        letter-spacing: 0.03em;
+        color: #fff;
+        background-color: var(--status-available);
+        padding: 0.15rem 0.4rem;
+        border-radius: var(--radius-md);
+    }
+
+    .card-price-regular {
+        font-size: 0.8rem;
+        color: var(--text-muted);
     }
 
     .card-actions {
diff --git a/src/components/ZohoFormularioHTML.astro b/src/components/ZohoFormularioHTML.astro
index 8aecdc4..a9f6a8b 100644
--- a/src/components/ZohoFormularioHTML.astro
+++ b/src/components/ZohoFormularioHTML.astro
@@ -190,7 +190,9 @@ const { talleres, tallerPreseleccionado, patronFondo = PATRON_TRIGO } =
         </button>
         <p class="form-note">
           *El pago y confirmación final se gestionan de forma directa con
-          nuestro equipo comercial.
+          nuestro equipo comercial. Precio de preventa disponible solo
+          pagando el taller completo al reservar; reservando con el 50% de
+          anticipo no aplica el descuento.
         </p>
       </form>
     </div>
diff --git a/src/content.config.ts b/src/content.config.ts
index 60cfcf3..7597595 100644
--- a/src/content.config.ts
+++ b/src/content.config.ts
@@ -16,6 +16,9 @@ const talleres = defineCollection({
     cuposDisponibles: z.number().int().nonnegative(),
     cuposTotales: z.number().int().positive(),
     precio: z.number().positive(),
+    // Precio de preventa: aplica SOLO si se paga el taller completo (100%) al reservar.
+    // Si la reserva se hace con el 50%, el descuento no aplica y el precio sigue siendo `precio`.
+    precioPreventa: z.number().positive().optional(),
     imagen: z.string(),
     imagenAlt: z.string(),
     destacado: z.boolean().default(false),
diff --git a/src/content/talleres/domina-el-arte-de-la-pizza.md b/src/content/talleres/domina-el-arte-de-la-pizza.md
index 44599f0..2f0790c 100644
--- a/src/content/talleres/domina-el-arte-de-la-pizza.md
+++ b/src/content/talleres/domina-el-arte-de-la-pizza.md
@@ -10,6 +10,7 @@ nivel: "Todo público"
 cuposDisponibles: 10
 cuposTotales: 10
 precio: 35.00
+precioPreventa: 30.00
 imagen: "https://images.unsplash.com/photo-1513104890138-7c749659a591?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80"
 imagenAlt: "Pizza artesanal recién horneada"
 destacado: false
diff --git a/src/content/talleres/torta-chilena-milhoja-napoleon.md b/src/content/talleres/torta-chilena-milhoja-napoleon.md
index 5546365..c53652e 100644
--- a/src/content/talleres/torta-chilena-milhoja-napoleon.md
+++ b/src/content/talleres/torta-chilena-milhoja-napoleon.md
@@ -10,6 +10,7 @@ nivel: "Todo público"
 cuposDisponibles: 10
 cuposTotales: 10
 precio: 35.00
+precioPreventa: 30.00
 imagen: "https://images.unsplash.com/photo-1578985545062-69928b1d9587?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80"
 imagenAlt: "Torta Chilena y Milhoja Napoleón"
 destacado: false
diff --git a/src/pages/talleres/[categoria]/[slug].astro b/src/pages/talleres/[categoria]/[slug].astro
index 2db7549..8aa99a2 100644
--- a/src/pages/talleres/[categoria]/[slug].astro
+++ b/src/pages/talleres/[categoria]/[slug].astro
@@ -30,6 +30,7 @@ const {
   cuposDisponibles,
   cuposTotales,
   precio,
+  precioPreventa,
   imagen,
   imagenAlt,
   categoria,
@@ -42,10 +43,14 @@ const fechaFormateada = fecha.toLocaleDateString("es-SV", {
   day: "numeric",
   month: "long",
 });
-const precioFormateado = new Intl.NumberFormat("en-US", {
+const formateadorUSD = new Intl.NumberFormat("en-US", {
   style: "currency",
   currency: "USD",
-}).format(precio);
+});
+const precioFormateado = formateadorUSD.format(precio);
+const precioPreventaFormateado = precioPreventa
+  ? formateadorUSD.format(precioPreventa)
+  : null;
 
 const ICONO_CHEF =
   '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M8 21h8"/><path d="M9 21v-5.5"/><path d="M15 21v-5.5"/><path d="M6 10a4 4 0 0 1 1.5-7.7A4.5 4.5 0 0 1 12 2a4.5 4.5 0 0 1 4.5.3A4 4 0 0 1 18 10c1.1 0 2 .9 2 2 0 1.7-1.3 3-3 3H7c-1.7 0-3-1.3-3-3 0-1.1.9-2 2-2z"/></svg>';
@@ -125,9 +130,24 @@ const ICONO_CHEF =
             </li>
             <li>
               <span>Precio:</span>
-              <span class="detalle-precio">{precioFormateado}</span>
+              <span class="detalle-precio">
+                {
+                  precioPreventaFormateado
+                    ? `${precioPreventaFormateado} (preventa, pago completo)`
+                    : precioFormateado
+                }
+              </span>
             </li>
           </ul>
+          {
+            precioPreventaFormateado && (
+              <p class="detalle-precio-nota">
+                Precio de preventa de {precioPreventaFormateado} pagando el
+                taller completo al reservar. Reservando con el 50% de anticipo
+                el descuento no aplica y el precio es de {precioFormateado}.
+              </p>
+            )
+          }
         </div>
       </section>
 
@@ -215,6 +235,13 @@ const ICONO_CHEF =
     color: var(--brand-primary);
   }
 
+  .detalle-precio-nota {
+    max-width: 480px;
+    margin-top: var(--space-xs);
+    font-size: 0.85rem;
+    color: #cbd5e0;
+  }
+
   .detalle-contenido {
     max-width: 700px;
     margin: 0 0 var(--space-lg);
